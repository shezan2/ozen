import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Every image is served from /public, so no remote patterns are needed.
  // Defaults for qualities, minimumCacheTTL and imageSizes are all fine here.
};

export default nextConfig;
