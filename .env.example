# Where the enquiry form POSTs. Any endpoint that accepts a JSON body works —
# Formspree, Basin, a Google Apps Script webhook, your own route handler.
#
# Leave it unset and the form falls back to handing the enquiry to WhatsApp with
# every field prefilled, rather than pretending to have sent it.
NEXT_PUBLIC_FORM_ENDPOINT=
